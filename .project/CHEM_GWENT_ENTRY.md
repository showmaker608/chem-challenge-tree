# 化学对决：娱乐优先入口

2026-09-11。本轮只调整游戏入口、引导和返回/再战流程，不修改挑战树主业务、学生数据或线上部署。

## 已实现

- 首页仅“开始对决”主按钮与“我的牌组”次入口。课程编号、教学大段说明、BGM 文案移除；帮助、声音保留右上角。
- 首次开始直接进入固定 10 张手牌的入门对局，不要求先组牌或换牌。界面明确标识入门对手，不伪装真人。
- 一次只显示一条上下文提示：出牌、组合、产物检验、留牌；电脑回合不显示提示。关闭引导不限制操作；帮助里可重新开启本局提示。
- 完成入门对决、主动关闭引导或保存自定义牌组后，后续从已保存的 16 张牌组进入自由对战，保留原有可选换牌阶段。
- 牌组保存后回到首页，不自动开战。对局结束主按钮“再来一局”，次按钮“调整牌组”；在对局中返回需确认，并回到游戏首页而非离开游戏。
- 原有三种练习放在帮助的“可选练习”中。化学事实、完整反应记录仍保留。
- 存储仅涉及 `chem-gwent-deck-v1` 与新键 `chem-gwent-intro-v1`；存储被阻止时仍可游玩，不涉及账号或学习进度。

## 本轮验证

- 首次点击直接开局；打出碳酸钙、稀盐酸生成气体，提示按上下文推进；关闭引导成功。
- 保存“守证反击”后回首页，重新打开游戏后进入自由对战，仍使用已保存牌组（起手出现该预设独有的精密测量）。
- 390px 下实际选牌、出牌、战术目标选择与执行；精密测量令氮气 5→8 分。访问学者给对手 +4，己方手牌 8→9、备用 6→4。
- 首页与对局均无页面横向溢出。默认窄窗口修复展示卡裁切和名称/公式拥挤，测试后已重置视口。
- `bun run build`、相关 ESLint、`bun scripts/test-gwent-entry.tsx`、200 场构筑模拟、100 场旧引擎模拟及全部化学链顺序测试通过。
- 截图：`output/chem-gwent-entry-v3/home-final.png`、`mobile-home.png`、`mobile-battle.png`。

范围限制：未做真人留存/平衡性实验、真实手机设备测试或完整无障碍审计；不是最终成品或已发布声明。

## 2026-09-12 换牌阶段可读说明 + 指定换牌

- 换牌（mulligan）阶段点击任意手牌或备用牌直接打开卡牌说明弹窗，不再只是选中。
- 备用 6 张牌在换牌阶段平铺展示；说明弹窗内按区域提供「换掉这张牌」（手牌）/「换这张进来」（备用牌）。
- 选定一出一进后点「确认交换」完成指定换牌，不再随机抽换；刚换入的牌本阶段不能再换（引擎层拦截）。
- 引擎：`swap` 动作新增可选 `with` 字段指定备用牌 id，缺省保持原牌堆顶行为；界面提示语同步显示「可换 N 张」与已选换出/换入。
- 测试：指定换牌进出正确、牌数守恒、换入牌不可再换、非法备用牌 id 拒绝；`test-gwent-duel.ts` / `test-chem-gwent.ts` / `test-gwent-entry.tsx` 全部通过，`tsc -b` 通过。

补充（同日）：组牌界面点卡改为弹窗说明（弹窗内可直接 ＋/− 调整数量）；修复 `.cg-overlay`/`.cg-dialog` 从未定义样式导致所有弹窗（卡牌说明、停牌确认、退出确认）以普通文档流内联渲染的问题，补全模态遮罩与居中弹窗样式。

## 2026-09-12 AI 难度分级 + 首次部署上线

- `chooseAI` 新增 `{ level, random }` 选项：normal 保持原全力贪心；easy 不再战略性弃局保牌，且从排名前 3 的正收益动作中随机选择（会犯错）。
- 首页新增「对手强度」选择：轻松（默认，它会犯错）/ 认真（全力计算），存 `chem-gwent-difficulty`；自由对战教练行显示当前对手强度。
- 测试：normal 弃局 / easy 不弃局、easy 动作恒合法、20 场 easy 完整对局；全部测试与 `tsc -b` 通过。
- **已部署**：`bun scripts/build-gwent.ts` → `tcb hosting deploy dist-game/ /games/chem-gwent/ -e xdkchem-d1g3f4ibw03b1b776`。
- 线上地址：https://xdkchem-d1g3f4ibw03b1b776-1372708082.tcloudbaseapp.com/games/chem-gwent/（index/JS/卡面均 200）。
- 注意：worktree 全部改动（含 codex 各轮迭代）仍未提交 git。

## 2026-09-12 游戏感打磨 + chemkun.com 确认

- 确认 chemkun.com 早已绑定同一 CloudBase 托管，游戏线上地址：https://chemkun.com/games/chem-gwent/
- 文案减负：删除卡牌弹窗里的拟人化免责声明（帮助与页脚保留一处）、停牌确认里的教程式补充；换牌面板从大段说明改为「点卡看说明 · 最多换 N 张」一行提示 + 换出/换入 chip；手牌/备用牌小字压缩为 4-6 字。
- 换牌交互：换入卡牌带翻转发牌动画（cg-deal-in，0.55s，支持 prefers-reduced-motion）；换牌面板去掉表单式边框背景，「开战」主按钮加大加字距。
- 已重新构建部署（38 个文件上传成功）。

## 2026-09-12 子路径资源 404 修复

- 生命宝石、桌面背景、卡面分数牌三处图片写死 `/gwent/...` 根路径，部署在 `/games/chem-gwent/` 子路径下全部 404（本地 dev 挂在根路径所以不暴露）。统一改为 `gameAsset()`（带 BASE_URL），CSS 中两处 url 改为 JS 内联 backgroundImage。
- 教练行文案「初次交锋 · 入门对手 · 第 1 局」→「入门练习赛 · 第 1 局」。
- 已重新部署（38 文件）；构建产物确认引用 `/games/chem-gwent/gwent/...`，线上 200。

## 2026-09-12 新卡 ×3 + 音乐 v2

- 试玩反馈：卡牌少、音乐难听。
- 新增 3 张物质牌（立绘为 image-to-image 以 peroxide-v2 为风格参考生成，已裁水印转 WebP）：
  - 红磷 P 4 分：燃烧耗氧测空气中氧气含量；镁带 Mg 5 分：燃烧耀眼白光；蒸馏水 H₂O 3 分：电解 2:1。
  - 均为纯物质牌，不引入新反应链，引擎无改动；collection 17→20，presets 不变，组牌可选池扩大。
- 音乐 v2：三角波快节奏连续音 → A 小调五声音阶 + 休止符 + 低通暖声滤波，步长 .32s→.44s，贝斯拉长，音量总线 .2→.15。合成器定位不变，真人民谣配乐仍为后续选项。
- 测试：collection 20、16 张立绘断言更新；全部测试与 tsc 通过。
